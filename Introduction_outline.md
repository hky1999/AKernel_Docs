## Introduction

## 一、首先定义论文的核心命题
建议整篇引言围绕这一句话展开：
Agent工作负载不再只是运行若干无状态容器，而是在数据中心内持续创建、派生、暂停、恢复和回收大量有状态执行环境。AKernel 将这种执行环境抽象为数据中心尺度的“进程”，并通过调度、存储和运行时的协同设计高效实现其完整生命周期。

这里必须把“有状态执行模型”定义清楚。一个Agent执行环境不仅包括容器镜像，还包括：
可变文件系统和代码仓库；
运行中的进程与内存状态；
数据、缓存和依赖；
凭证、网络策略和工具服务；
资源配额及运行时配置。
其关键特征不是简单的启动和销毁，而是环境需要被派生、等待、挂起、恢复、迁移和复用。


## 二、建议的引言段落结构

### 第一段：从单机工具使用到数据中心执行

说明Agent基础设施接口正在发生变化：
早期Agent主要控制浏览器、终端或单台虚拟机；
现在的编程、运维、数据分析、强化学习和自动评测任务，会并发启动大量隔离环境；
Agent实际上正在根据任务动态组织计算、数据和外部服务。
本段结论：
> Agent正在从“使用一台计算机”转变为“驱动一个数据中心”。

这一段只描述趋势，不要过早讨论 Kubernetes 和具体机制。


### 第二段：提炼新的有状态执行模型
从真实工作负载中总结四个本质特征：
环境有状态：准备好的代码、依赖、文件和运行状态具有持续价值。
生命周期丰富：环境需要创建、派生、暂停、恢复和回收。
负载高度突发：短时间内可能创建成百上千个异构环境。
执行具有间歇性：环境经常等待模型或外部服务，但仍占用资源。
本段结论：
> Agent基础设施的基本对象不是无状态函数或长期运行的服务，而是可移动、可派生、可恢复的有状态执行环境。

这一段是整篇论文最重要的“问题定义”。

### 第三段：现有云平台为什么不匹配
建议把问题分成两层。
抽象层不匹配：
Kubernetes 以 Pod、Deployment 等资源对象及声明式状态为中心；
无服务器平台通常以无状态函数调用为中心；
Agent需要的是直接操作执行环境生命周期的少量原语。
实现层不匹配：
大量异构镜像会造成仓库、网络和节点磁盘瓶颈；
环境状态与节点绑定，难以快速派生、迁移和恢复；
等待期间继续占用计算资源，导致成本和利用率问题。
表述上不要说“Kubernetes 不能运行Agent”，而应说：
> Kubernetes 可以承载这些工作负载，但没有提供与其执行模型匹配的抽象和端到端优化。

“YAML 太长”“Agent不会管理集群”可以作为辅助现象，不能作为核心科学论据。


### 第四段：明确三个系统挑战
将现有零散问题收敛成三个挑战，并与后面的设计一一对应。
快速物化执行环境
系统必须快速准备镜像、代码、依赖、数据、凭证和网络策略。真正指标不是容器启动完成，而是Agent能够执行第一条有效动作的时间。

高效管理和移动环境状态
系统需要统一管理镜像、文件和运行状态，使环境能够跨节点检查点、恢复、派生和迁移，而不必每次从头构建。

根据生命周期调度和回收资源
系统必须识别运行、等待、挂起等状态，在延迟、隔离强度和成本之间进行权衡，并适配不同沙箱运行时。

这里可以首次引出两个核心指标：
有效动作就绪时间：从请求环境到Agent完成第一条有效动作的时间；
Agent有效吞吐：在给定成本和服务目标下正确完成的任务数。


### 第五段：提出 AKernel 的核心抽象
正式引出 AKernel：
> AKernel 将一个完整的Agent执行环境抽象为数据中心尺度的进程，并向上提供少量进程式生命周期原语，向下统一管理计算节点、分布式状态和沙箱运行时。

可以使用操作系统类比，但必须说明这是抽象和机制上的对应关系：
进程对应沙箱环境；
可执行文件和页缓存对应镜像及按需加载；
派生和执行对应环境创建与复用；
挂起和恢复对应检查点与恢复；
进程调度对应跨节点资源调度。
具体原语的名称和语义需要后续统一。目前草稿中 spawn、fork、exec 的含义存在混用，正式写作前必须明确。


### 第六段：设计一——统一的执行环境与控制平面
重点不是“提供了一个 SDK”，而是：
执行环境具有独立于物理节点的逻辑身份；
少量原语描述其生命周期状态转换；
全局控制平面负责节点选择、资源分配和运行时选择；
调度、数据和安全策略不直接暴露给Agent。
核心价值是：把复杂资源配置转换为稳定的执行语义。


### 第七段：设计二——统一的分布式状态平面
将现有镜像、对象存储、分层文件系统、按需加载和点对点分发统一到一个设计中：
统一管理基础镜像、可变文件、对象和检查点；
只按需读取环境真正访问的数据；
使用缓存和点对点传播吸收突发流量；
避免环境状态绑定到单个节点。
这一设计同时服务于启动、派生、恢复和迁移，而不应只描述成“冷启动优化”。


### 第八段：设计三——生命周期感知的调度与恢复
说明 AKernel 如何利用环境状态提高资源效率：
活跃环境继续运行；
等待模型或外部事件的环境可以释放部分资源；
长时间空闲环境自动检查点并回收计算资源；
后续请求到来时快速恢复；
根据兼容性、隔离强度和成本选择合适的沙箱运行时。
这样，检查点/恢复不再是孤立功能，而是连接状态管理、调度和成本控制的关键机制。


### 第九段：评测结论
引言中的评测摘要应围绕核心命题，而不是只报告毫秒级启动：
生产工作负载是否确实呈现突发、有状态和长等待特征；
相比优化后的 Kubernetes、无服务器平台或沙箱系统，有效动作就绪时间降低多少；
高并发启动时，仓库流量、节点流量和存储占用降低多少；
检查点/恢复如何改善资源利用率和任务成本；
各项机制分别贡献多少；
系统是否已经在真实生产环境长期运行。

###  第十段：贡献总结
建议最终归纳为三项贡献：
基于生产工作负载，识别并定义数据中心尺度的Agent有状态执行模型。
提出 AKernel 的进程式执行环境抽象，以及调度、状态存储和运行时协同设计。
通过真实部署和大规模实验，证明其在有效动作延迟、突发扩展和资源成本上的收益。

## 三、现有内容如何取舍
建议保留并加强：
从“计算机使用”到“数据中心使用”；
沙箱对应进程的核心类比；
镜像异构、突发启动和长时间等待；
按需加载、点对点分发和检查点/恢复；
有效动作就绪时间和Agent有效吞吐。
建议移到后续章节：
仓库上下行流量、软件包镜像源和网络地址转换等细节；
跨数据中心处理器兼容问题；
各种运行时的具体名称；
审计、访问控制、可观测性和健康检查；
分布式文件系统与点对点系统的具体实现差异。
建议删除或谨慎使用：
“Agent缺乏管理集群的能力”；
“Deployment YAML 往往几百行”；
“技能会占用上下文、模型会犯错”；
“等待模型回复等于 epoll”；
“扩缩容等于处理器热插拔”；
暂无数据支撑的“分钟级”和“毫秒级”结论。

最终，引言应形成一个紧密闭环：
> Agent产生新的有状态执行模型，而非简单增加了容器数量；现有平台缺少相应的生命周期抽象和高效实现；AKernel 将环境抽象为数据中心进程，并以统一状态平面和生命周期感知调度实现这一模型。